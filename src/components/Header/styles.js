import styled from "styled-components";

export const HeaderWrapper = styled.div`
width: 100%;
height: 100%;
background: url(${props => props.imageBackground}) no-repeat bottom;
background-size: cover;

`